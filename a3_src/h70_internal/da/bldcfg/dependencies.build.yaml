title:
  Build restriction configuration file for building third-party dependencies.

options:

  loglevel_overall:                   DEBUG
  loglevel_file:                      DEBUG
  loglevel_console:                   DEBUG

  dep_build_exclusion:                Null
  dep_build_limitation:               Null

steps:

  enable_dep_fetch_src:               True
  enable_dep_build:                   True
  enable_main_build:                  False
